import { createSlice } from "@reduxjs/toolkit";

const initialState= {
    values: []    
}

export const inCartSlice = createSlice({
    name: "inCart",
    initialState,
    reducers: {

    }
})

export default  inCartSlice.reducer